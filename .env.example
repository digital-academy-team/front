# Frontend environment variables (Vite). Copy to `.env.local` for local dev.
#
# Backend base URL the SPA calls. Default (unset) = the PRODUCTION backend
# (https://api.digital-academy.live). For local dev against a local backend, set:
#   VITE_API_URL=http://localhost:8000
VITE_API_URL=
