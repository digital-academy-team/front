# Loaded automatically by `vite build` (production builds).
# The deployed frontend talks to the production backend over HTTPS.
# Change this to wherever the backend is hosted.
VITE_API_URL=https://api.digital-academy.live
